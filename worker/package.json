{
  "name": "calorie-app-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute calorie-app-db --file=./schema.sql",
    "db:init:remote": "wrangler d1 execute calorie-app-db --remote --file=./schema.sql",
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^1.6.1"
  },
  "dependencies": {
    "@sentry/cloudflare": "^10.45.0"
  }
}
